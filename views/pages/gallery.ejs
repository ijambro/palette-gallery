<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("../partials/head"); %>
    <link href="/css/gallery.css" rel="stylesheet" type="text/css">
</head>
<body class="container">

<header>
    <%- include("../partials/header"); %>
</header>

<main>
    <div class="jumbotron">
        <h1>Your full image gallery</h1>
        <p>Click any image to see its color palette</p>
    </div>
    <div class="grid well well-lg">
        <ul>
        <% for (let i=0; i < imagesData.images.length; i++) { %>
            <% let imageData = imagesData.images[i]; %>
            <li>
                <a href="/images?name=<%= imageData.name %>">
                    <img src="<%- imageData.url %>">
                </a>
            </li>
        <% } %>
        </ul>
    </div>
</main>

<footer>
    <%- include("../partials/footer"); %>
</footer>

</body>
</html>